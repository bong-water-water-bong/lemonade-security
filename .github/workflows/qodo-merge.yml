name: qodo-merge
on:
  pull_request:
    branches: [main]
  workflow_dispatch:
    inputs:
      pr_number:
        description: "PR number to review (leave empty for PR event)"
        required: false
        type: string

jobs:
  qodo-merge:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write
      contents: read
    steps:
      - name: Qodo Merge Review
        run: |
          echo "Qodo merge review is optional for this repository."
          echo "Skipping because qodo-ai/qodo-merge is not a resolvable public GitHub Action."
          echo "Keep PR-Agent and CI as the required automated gates."
